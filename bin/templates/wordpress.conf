server {
    listen 80;
    server_name <%= wordpress_domains.join(' ') %>;

    include /etc/nginx/conf.d/wordpress.d/*.conf;

    location / {
        proxy_pass http://wordpress;
        proxy_redirect off;
        proxy_set_header Host $host;
    }
}

upstream wordpress {
    server wordpress:80;
}
