version: "2"
services:
  nginx:
    image: nginx
    ports:
      - 80:80
      - 443:443
    volumes:
      - ./conf.d:/etc/nginx/conf.d
      - ${TRAMBAR_MEDIA_FOLDER}:/var/cache/media
      - ./snakeoil:/etc/ssl/snakeoil
